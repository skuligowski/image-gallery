title: User
type: object
properties:
  username:
    type: string
    description: Username
  admin:
    type: boolean
    description: Set to true if the user has administration privileges
  guest:
    type: boolean
    description: Set to true if authentication is switched off and any user can access the gallery
required:
- username
- admin
